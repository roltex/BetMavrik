{
  "name": "betmavrik-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "cd backend && npm ci",
    "build-backend": "cd backend && npm run build",
    "list-dist": "ls -la backend/dist/ || echo 'dist folder not found'",
    "build": "npm run install-backend && npm run build-backend && npm run list-dist",
    "start": "cd backend && npm run start:prod",
    "dev": "cd backend && npm run start:dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 